[package]
name = "analytic-intelligence"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1.0", features = ["derive"] }
trust-dns-resolver = { version = "0.23", features = ["dns-over-rustls"] }